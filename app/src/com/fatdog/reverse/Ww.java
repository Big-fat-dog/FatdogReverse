package com.fatdog.reverse;

// 流沙河的诱饵：近亲假标记（shatter → scatter）。用它派生钥匙构造的请求
// 一律 403。真标记在 so 里以 UTF-16 码元躺着，strings 默认看不见；
// 而这一个明文躺在全局区等粗心的猎物上钩。
public class Ww {
    private Ww() {
    }

    public static final String FAKE_KEY = "Fatdog_scatter";
}
