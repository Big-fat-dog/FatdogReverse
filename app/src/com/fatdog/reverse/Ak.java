package com.fatdog.reverse;

// 关卡 34 的诱饵：与真钥一字之差的近亲（grumpy → sore），名字也像密钥库。
// 万法归墟里没有捷径——真密钥在动态注册的无名函数里，还带着全套守卫。
public class Ak {
    public static final String FAKE_KEY = "Fatdog_sore";

    private Ak() {
    }
}
